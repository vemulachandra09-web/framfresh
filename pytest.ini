[pytest]
testpaths = ui_automation/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
cache_dir = ui_automation/.pytest_cache
markers =
    smoke: quick UI checks for critical journeys
    e2e: tests that require the frontend, backend, and seed data to be running
